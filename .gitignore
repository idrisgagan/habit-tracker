node_modules
dist
*.local
.DS_Store

# TypeScript incremental build cache — regenerated by `tsc -b` on every build
tsconfig.tsbuildinfo

# Stop-hook activity log. Must stay ignored: if tracked, writing it would dirty
# the tree and the hook would commit its own log on every stop, forever.
.claude/hooks/autocommit.log
